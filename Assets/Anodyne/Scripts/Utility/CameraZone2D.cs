using UnityEngine;
using System.Collections;

public class CameraZone2D : MonoBehaviour {

    // Use this for initialization
    void Start() {

    }

    /*
    public enum CameraZone2DShape { _1x2, _2x1, _2x2 };
    public CameraZone2DShape shape = CameraZone2DShape._2x2;
     
    bool isSquare = true;
    int zoneWidth = 1;
    int zoneHeight = 1;
    */
    private void OnValidate() {



      //  Vector2 pos = transform.position;
       // int roomX = Mathf.FloorToInt(pos.x / 10f);
        //int roomY = Mathf.FloorToInt(pos.y / 10f);
        //string newname = roomX.ToString() + roomY.ToString() + " ";

        
            
    }
}
